The instructions assume you are in the directory this file resides in.

1. set up the tools:
    ```shell
    ./setup_utils.sh
    ```
3. run the generator:
    ```shell
    ./generate.sh
    ```

The output file will appear in this directory and will be called `world_reviews.obf`.
