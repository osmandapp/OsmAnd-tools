# Two CI jobs can compile OsmAnd-shared in one shared Kotlin daemon at the same time;
# the 512m Gradle default is no longer enough (OutOfMemoryError in compileKotlinJvm).
org.gradle.jvmargs=-Xmx2g -XX:MaxMetaspaceSize=768m
kotlin.daemon.jvmargs=-Xmx2g
